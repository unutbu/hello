from setuptools import setup

setup(
    name = 'hello',
    version = '0.1',
    description = 'An example of an installable program',
    author = 'unutbu',
    url = '',
    license = 'MIT',
    py_modules = ['hello'],
)
